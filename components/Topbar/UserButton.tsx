import React from 'react';
import Image from 'next/image';

const UserButton = async () => {

  return (
    <div className='flex aspect-square h-10 items-center justify-center rounded-full border-2 border-zinc-800 bg-zinc-900 transition-colors hover:border-pink-600 hover:text-zinc-200 relative overflow-hidden'>

    </div>
  );
};

export default UserButton;
