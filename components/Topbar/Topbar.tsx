import UserButton from './UserButton';
import Navigation from './Navigation';

const Topbar = () => {
  return (
    <div className='flex h-16 w-full flex-shrink-0 items-center justify-between rounded-t-md bg-zinc-800 px-6'>
      <Navigation/>
      <div>
        
      </div>
    </div>
  );
};

export default Topbar;
